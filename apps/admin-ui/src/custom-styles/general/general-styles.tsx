export const GeneralStyles = () => {
  // Form with
  // Logo URL: input box
  // shows preview, bounded size, after save
  // Favicon URL: input box
  // shows preview, bounded size, after save
  // Save/Cancel button

  return <div>General Styles</div>;
};
